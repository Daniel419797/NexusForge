import Link from "next/link";

interface Props {
    projectId: string;
    currentPath?: string;
}

export default async function ProjectNav({ projectId }: Props) {
    return (
        <nav className="">
        </nav>
    );
}
